# GSAP

GSAP is the primary orchestration engine for complex animation.

Use:
- gsap.to
- gsap.from
- gsap.fromTo
- gsap.timeline
- stagger
- easing
- callbacks
- context/cleanup where appropriate

Use GSAP for coordinated multi-element animation and cinematic sequences.
