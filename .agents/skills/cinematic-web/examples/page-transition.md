# Cinematic Page Transition

Possible architecture:

navigation
→ transition state
→ outgoing scene animation
→ route change
→ incoming scene animation
→ restore scroll state

Use Barba.js only when its transition architecture provides value.
