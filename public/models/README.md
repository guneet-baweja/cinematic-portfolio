# /public/models

Reserved for a future GLTF/GLB model if you want to replace the
procedural hero object in `src/components/SceneCanvas/TimelineRibbon.tsx`
with an authored 3D asset. Load it with Drei's `useGLTF` hook.
