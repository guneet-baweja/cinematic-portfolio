# /public/images

Poster images shown before a project's video loads, and used by the
Real Estate section's shader-distorted hero image. Generated placeholder
SVGs are already here — replace them with real JPG/PNG stills (same
filenames, or update the paths in `src/data/projects.ts`).
