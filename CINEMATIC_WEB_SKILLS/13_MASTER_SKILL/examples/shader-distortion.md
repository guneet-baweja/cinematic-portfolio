# Shader Distortion

A common pattern:

scroll velocity
→ uniform
→ fragment/vertex shader
→ UV displacement
→ image distortion

Use uniforms for dynamic values instead of rebuilding shader programs.
