# Motion

Motion is useful for UI-oriented and React-oriented animation.

Use it selectively for:
- component transitions
- layout animation
- UI interactions

If GSAP already owns the motion architecture, avoid unnecessary duplication.
